import React from 'react'
import styled from 'styled-components'
import { colours } from 'utils/globalStyles'

const Footer = styled.section`
  background-color: ${colours.primary.shaded};
  height: 200px;
  width: 100%;
`

export default () => (
  <Footer />
)
